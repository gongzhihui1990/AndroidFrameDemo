<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">

    <solid android:color="@color/main3" />

    <stroke
        android:width="2dp"
        android:color="@android:color/black" />

    <padding
        android:top="@dimen/round_padding"
        android:left="@dimen/round_padding"
        android:right="@dimen/round_padding"
        android:bottom="@dimen/round_padding"
        />
    <size
        android:width="@dimen/round_size"
        android:height="@dimen/round_size" />

</shape>